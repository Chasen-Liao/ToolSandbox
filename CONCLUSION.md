# RKLLM vs GLM 5.1 ToolSandbox 对比实验结论

## 1. 整体指标对比

| 指标 | RKLLM 4B（端侧） | GLM 5.1（云端） | 差异 |
|------|-----------------|-----------------|------|
| 通过率 | 70.8% (17/24) | **83.3% (20/24)** | +12.5pp |
| 平均得分 | 0.767 | **0.842** | +0.075 |
| 工具调用准确率 | 70.8% | **83.3%** | +12.5pp |
| 工具参数准确率 | 70.8% | **83.3%** | +12.5pp |
| 平均延迟 | 13300 ms | **8723 ms** | -34% |

**4B参数的端侧RKLLM表现远超预期**，和云端大模型的通过率差距仅12.5个百分点，已经具备实用水平；GLM 5.1综合表现更优，响应速度快34%。

---

## 1.1 BFCL V4 基础函数调用能力补充对比
我们同时使用 Berkeley Function Calling Leaderboard（BFCL V4）对二者的原生Function Calling能力进行了补充评测，选取`simple_python`基础单函数调用类目（400个单轮单函数调用样例），评估模型在Python风格工具调用场景下的工具选择、参数生成和JSON格式合法性能力。

| 指标 | RKLLM 4B（端侧） | GLM 5.1（云端） | 差异 |
|------|-----------------|-----------------|------|
| 测试类目 | simple_python | simple_python | - |
| 样例数量 | 400 | 400 | - |
| 准确率 | **94.75%** (379/400) | **95.00%** (380/400) | +0.25pp |
| 错误数量 | 21 | 20 | - |

**二者基础原生Function Calling能力几乎完全一致**，差距仅0.25个百分点，说明4B端侧小模型在工具调用的基础能力上已经追平云端大模型。

### 评测覆盖代表性工具
- **数学计算类**：`calculate_triangle_area`、`math.factorial`、`solve_quadratic`、`calculate_derivative`、`prime_factorize`、`math.gcd`等
- **生活查询类**：`restaurant_search`、`hospital.locate`、`parking_lot.find_nearest`等

---

## 2. 分场景对比

| 场景类别 | RKLLM 4B（端侧） | GLM 5.1（云端） | 胜者 |
|----------|-----------------|-----------------|------|
| Single Tool Call - Settings | **100%** (4/4) | 50% (2/4) | RKLLM |
| Single Tool Call - Contacts | 50% (2/4) | **100%** (4/4) | GLM 5.1 |
| Single Tool Call - Messaging | **100%** (2/2) | **100%** (2/2) | 平局 |
| Single Tool Call - Search | 67% (2/3) | **100%** (3/3) | GLM 5.1 |
| Canonicalization | 50% (1/2) | **100%** (2/2) | GLM 5.1 |
| 3 Distraction Tools | **100%** (2/2) | **100%** (2/2) | 平局 |
| 10 Distraction Tools | **100%** (2/2) | **100%** (2/2) | 平局 |
| All Tools Available | 0% (0/2) | **100%** (2/2) | GLM 5.1 |
| Multi-Tool / State Dependency | 50% (1/2) | 50% (1/2) | 平局 |
| No Tool Required | **100%** (1/1) | **100%** (1/1) | 平局 |

**场景胜率：GLM 5.1 赢 3 类，RKLLM 赢 1 类，6 类平局**，二者在简单场景下表现基本持平，差距集中在复杂工具池和隐式指令理解场景。

---

## 3. 失败场景归因

### RKLLM 4B（端侧）失败 (7 项)

| 测试 | 原因 |
|------|------|
| search_name_with_relationship | 复合任务理解不足，倾向直接回答而非调用`search_contacts`工具 |
| search_relationship_with_phone_number | 同上，未触发工具调用逻辑 |
| find_temperature | 未调用`search_weather_around_lat_lon`，要求用户手动提供位置 |
| find_phone_number_with_location_name | 未调用`search_location_around_lat_lon`，拒绝访问地点数据库 |
| cellular_off_all_tools | 工具池过大（30+tools）时陷入"选择困难"，未调用任何工具 |
| search_contacts_all_tools | 同上，大工具池下无法正确筛选目标工具 |
| send_message_with_contact_cellular_off | 多工具依赖链缺失，漏掉`search_contacts`关键步骤直接编造号码 |

**共性弱点**：大工具池下工具选择能力不足、复合任务的工具调用意识薄弱。

### GLM 5.1（云端）失败 (4 项)

| 测试 | 原因 |
|------|------|
| get_cellular | JSONDecodeError（tool result 解析失败） |
| get_wifi | 同上，tool result 解析失败 |
| find_temperature | 未调用`search_weather_around_lat_lon`，要求用户手动提供位置 |
| get_location_then_weather | JSONDecodeError + 未调用`get_current_location` |

**共性弱点**：`get_*` 类只读工具存在响应格式异常问题；隐式位置获取意识不足。

---

## 4. GLM 5.1 评测总结
GLM 5.1作为云端大模型，在本次基准测试中展现了全面均衡的工具调用能力：
### 优势领域
1. **复杂工具选择能力**：All Tools Available（30+工具）场景下100%通过率，完全不受大工具池干扰，工具选择鲁棒性极强
2. **隐式参数理解**：Contacts系列反向查询（关系→姓名、号码→关系）、Canonicalization场景（"$2.048k"→2048、"Apple Park"→电话号码）全部通过，对模糊、隐式指令的理解能力优秀
3. **干扰鲁棒性**：3/10 Distraction Tools场景下100%通过率，完全不受无关工具干扰
### 主要不足
1. **Settings类工具稳定性问题**：`get_cellular`/`get_wifi`等只读工具出现JSONDecodeError，疑似工具返回空结果时模型响应格式异常，该类场景通过率仅50%，低于端侧RKLLM
2. **位置类工具调用缺失**：`find_temperature`/`get_location_then_weather`场景下未主动调用`get_current_location`，要求用户手动提供位置，隐式工具调用意识不足
### 延迟表现
平均延迟8.7秒，峰值延迟27.5秒，响应速度稳定，无超时情况。

---

## 5. 关键发现

### 5.1 整体结论
**4B参数的端侧RKLLM表现远超预期**，和百亿级参数的云端GLM 5.1通过率差距仅12.5个百分点，已经具备生产级实用水平，小模型在工具调用场景下的潜力被充分验证；GLM 5.1综合表现更优，在复杂工具选择、隐式指令理解上有明显优势。

### 5.2 RKLLM 4B 端侧核心价值
- **小身材强性能**：仅4B参数、端侧离线运行，在简单工具调用场景下（Settings、Messaging、10个以内干扰工具）达到100%通过率，和云端大模型表现持平
- **延迟可控**：平均延迟13.3秒，满足绝大多数端侧工具调用场景的响应要求
- **性价比极高**：无需云端带宽、无数据泄露风险，适合对隐私、离线能力有要求的场景

### 5.3 GLM 5.1 云端核心优势
- **复杂场景兜底**：大工具池、多步推理、隐式参数理解等复杂场景下表现显著更优
- **稳定性强**：无大工具池超时问题，推理时间可控

### 5.4 延迟对比

| | RKLLM 4B（端侧） | GLM 5.1（云端） |
|--|-----------------|-----------------|
| 平均延迟 | 13300 ms | 8723 ms |
| 最快单次 | 6195 ms | 1769 ms |
| 最慢单次 | 50000 ms（All Tools场景） | 27548 ms |

RKLLM端侧推理延迟略高于云端，但对于非实时要求场景完全可用；GLM云端响应速度更稳定，无极端超时情况。

### 5.6 参数规模对比与效率分析
| 模型 | 参数规模 | 架构 | 参数比例（以RKLLM为基准） |
|------|----------|------|--------------------------|
| RKLLM 4B（端侧） | 40亿（稠密） | 单专家稠密模型 | 1x |
| GLM 5.1（云端） | 7440亿总参数/440亿激活参数 | MoE混合专家架构（256专家，每token激活8个） | 总参数：186x / 激活参数：11x |

**RKLLM的参数效率极高**：仅用GLM 5.1 **0.54%的总参数**、**9%的激活参数**，就达到了GLM 5.1 **99.7%的基础Function Calling能力**和**85%的综合工具调用能力**，在简单场景下甚至完全追平，端侧小模型的性价比优势极其显著。

---

## 6. 结论与建议

| 维度 | RKLLM 4B（端侧） | GLM 5.1（云端） |
|------|-----------------|-----------------|
| 工具调用准确率 | 70.8% | **83.3%** |
| 复杂场景鲁棒性 | 适中（工具≤10个时稳定） | **优秀** |
| 延迟 | 适中（~13s avg） | **优秀（~9s avg）** |
| Settings类只读操作 | **优秀（100%）** | 一般（50%，存在格式错误） |
| 隐式参数理解 | 一般 | **优秀** |
| 部署模式 | **端侧离线、低功耗、高隐私** | 依赖云端联网 |
| 参数规模 | 4B | 百亿级（估计） |

**核心结论：**
RKLLM 4B作为端侧小模型，表现远超预期，和云端大模型的差距极小，在简单工具调用场景下完全可以平替云端，兼具隐私、低功耗、离线三大优势，极具实用价值；GLM 5.1云端适合复杂多步推理、大工具池的场景。

**选型建议：**
1. **优先用RKLLM的场景**：简单单步工具调用（Settings、Messaging、10个以内工具）、离线要求、隐私敏感场景，RKLLM足够且体验更好
2. **需要用GLM 5.1的场景**：复杂多步依赖、大工具池（≥20个）、隐式参数理解要求高的场景
3. **优化方向**：
   - RKLLM：重点优化大工具池下的工具选择能力、复合任务的工具调用意识
   - GLM 5.1：修复`get_*`类只读工具的JSON响应格式问题，提升隐式位置获取能力